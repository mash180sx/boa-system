// Generated by CoffeeScript 1.3.1
(function() {
  var OperationHelper;

  OperationHelper = require('apac').OperationHelper;

  /*
  ##
  ## apac (Amazon Product Advertising Client)による Amazon 情報取得
  ## 
  ##  ・JAN(EAN)により、最大10個まで検索
  ##  
  ## input      : conf コンフィギュレーション
  ##  = {
  ##    awsId: conf.awsId,
  ##    awsSecret: conf.awsSecret,
  ##    assocId: conf.assocId
  ##  }
  ## input      : JANS 配列（最大10件）
  ## output : callback(err, items) コールバック関数 items は Amazon 情報リスト(配列)
  */


  exports.getApaclist = function(conf, JANS, cb) {
    var itemId, opHelper, params;
    params = {
      awsId: conf.awsId,
      awsSecret: conf.awsSecret,
      assocId: conf.assocId,
      endPoint: conf.endPoint
    };
    if (conf.proxy != null) {
      params.proxy = conf.proxy;
    }
    if (conf.port != null) {
      params.port = conf.port;
    }
    JANS = JANS.slice(0, 10);
    opHelper = new OperationHelper(params);
    itemId = JANS.join(',');
    console.log(itemId);
    return opHelper.execute('ItemLookup', {
      Condition: 'All',
      IdType: 'EAN',
      SearchIndex: 'All',
      ItemId: "" + itemId,
      ResponseGroup: "ItemAttributes,OfferSummary,SalesRank"
    }, function(err, results) {
      var i, items, result, valid, _i, _len, _ref, _ref1, _ref2, _ref3, _ref4, _ref5, _ref6, _ref7;
      valid = results.Items.Request.IsValid;
      console.log(valid);
      if (valid === 'True') {
        items = [];
        _ref = results.Items.Item;
        for (i = _i = 0, _len = _ref.length; _i < _len; i = ++_i) {
          result = _ref[i];
          items[i] = {
            JAN: (_ref1 = result.ItemAttributes) != null ? _ref1.EAN : void 0,
            ASIN: result.ASIN,
            url: result.DetailPageURL,
            title: (_ref2 = result.ItemAttributes) != null ? _ref2.Title : void 0,
            author: (_ref3 = result.ItemAttributes) != null ? _ref3.Author : void 0,
            "new": Number((_ref4 = result.OfferSummary) != null ? (_ref5 = _ref4.LowestNewPrice) != null ? _ref5.Amount : void 0 : void 0),
            old: Number((_ref6 = result.OfferSummary) != null ? (_ref7 = _ref6.LowestUsedPrice) != null ? _ref7.Amount : void 0 : void 0),
            rank: Number(result.SalesRank)
          };
        }
        return cb(err, items);
      } else {
        return cb(err, results);
      }
    });
  };

}).call(this);
